# Issue #58 — pre-commit review

**Current verdict: both review findings resolved and verified.** The original
review requested changes for two reproduced defects; the resolution is below.
Review base: `cb82deb`; scope includes
the importer, new feedback module, HUD/main integration, CSS, dedicated browser
smoke and accompanying documentation. The initial review made no implementation
changes; the subsequent fix pass addressed both findings.

## Verified resolution

- Confirmation results now distinguish `yes`, `no` and `aborted`. HUD teardown
  and duplicate requests produce `aborted`; the Delete handler dispatches no
  command for that result. Explicit No/Escape retain their existing semantics.
  The browser regression opens a mixed-selection dialog and calls the real
  presentation rebuild, then asserts the synchronization hash is unchanged and
  both entities survive.
- Owned notifications now establish a persistent stacking context with
  `isolation: isolate`. At opacity one, the browser regression measures sRGB
  `[78,76,73,255]` with the Surround and `[240,240,240,255]` with it hidden, over
  the controlled battlefield plane. The background remains visible after the
  fade rather than merely existing in the DOM.
- `npx tsx tools/feedback_smoke.mts` and
  `OPEN_FALLBACK=1 npx tsx tools/feedback_smoke.mts` both pass with the new
  lifecycle regression; the imported run additionally checks background pixels.
  Existing player-response and notification-expiry checks still pass.
- Final corrected-tree gate: **GREEN**, 775 TypeScript tests, build, 105 Python
  import tests and general browser debug smoke. Log:
  `.local/issue58-review-fixes-gate.log`. No test timeout was widened.
- This checkpoint resolves both findings. The scope/reference-calibration
  limitations below still apply; it does not close the rest of #58.

## Original findings (resolved)

### 1. P1 — HUD teardown can delete units without a player response

**Locations:** `src/view/hud.ts:183–185` (`destroy`),
`src/main.ts:972–980` (`remove`), `src/main.ts:1908–1910` (presentation rebuild).

`destroy()` resolves a pending confirmation with `false`. The Delete handler
interprets `false` as the player choosing No, which intentionally deletes the
unflagged members of a mixed selection. A same-match presentation rebuild—used
by view hot reload—therefore sends a destructive public command without a click
or key response. The `game !== match` guard does not help because this rebuild
preserves the simulation object.

**Reproduction:** start a paused imported match; select the player's town center
and a villager; press Delete; leave the dialog open; invoke the existing
`rebuildPresentation()` entrypoint used by HMR. Before rebuilding, the villager
is alive at 25 HP. Afterwards it is absent from the live debug entity reply;
the town center remains at 2400 HP. The probe only stages the initial snapshot
and calls the real presentation lifecycle; it does not delete the villager
itself.

**Required change:** distinguish lifecycle abort from an explicit No. Teardown
must settle/discard the pending operation without invoking either deletion
branch. Preserve the existing deliberate No behaviour for mixed selections.
Add a same-match rebuild regression that asserts the simulation checksum and
entity membership remain unchanged.

### 2. P2 — notification background disappears at the end of the fade

**Location:** `src/view/style.css:250–251`.

The Surround has `z-index: -1`, while `.owned-notifications` removes the parent's
transform. During the fade, opacity below one creates a stacking context, so
the background can appear. At opacity one the parent has no stacking context;
its negative-z child falls behind the battlefield. Imported notifications thus
lose their background precisely when they reach their stable visible state.

**Reproduction:** in the real imported HUD at 1280×800, show a message and wait
for computed opacity one. Place an opaque diagnostic battlefield plane behind
the HUD to avoid animation changing the measurement. Sample an empty centre
portion of the panel at screen (113,160). The screenshot readback is sRGB:

| Condition | RGBA bytes |
|---|---|
| Current CSS, fully visible message | 240, 240, 240, 255 |
| Only add `isolation: isolate` to the message parent | 78, 76, 73, 255 |
| Explicitly hide the Surround | 240, 240, 240, 255 |

The unmodified result is exactly the no-background result. This is a layering
defect, not a reference-colour calibration dispute.

**Required change:** give the message panel a persistent stacking context, or
place background/text on explicit nonnegative layers. Add a browser check that
measures background contribution after opacity has reached one, not merely the
presence of nine DOM cells and background-image URLs.

## Initial review verification assessment

- The earlier final gate is green: 775 TypeScript tests, build, 105 Python import
  tests and the general real-browser debug smoke
  (`.local/issue58-gate-final.log`).
- Reran `npx tsx tools/feedback_smoke.mts` and
  `OPEN_FALLBACK=1 npx tsx tools/feedback_smoke.mts` during review. Both pass.
  They cover actual attack/research alerts, Yes/No/Escape, repeated opening,
  mixed-selection responses, text escaping, bounded messages and overlapping
  expiry. They do not exercise lifecycle abort or verify visible panel pixels.
- The dedicated feedback smoke is not called by `tools/gate.sh` or an npm
  script; its evidence is currently a separate manual run. Keep its invocation
  explicit in acceptance verification.
- Shared-play reconnect/resynchronization with a pending modal was reviewed
  statically but not exercised in a two-client browser session. No new
  shared-play correctness claim is made here.
- No source files or fixtures were edited to force the findings. A private
  Vite server on port 5278 exposed the existing HUD/rebuild entrypoints; a
  restored paused fixture was checked against its initial tick. Browser and
  server were closed after the probe.

## Scope and source fidelity

The source audit correctly distinguishes the empty `GameMsgPanel` anchors and
general yes/no viewport from actual notification/confirmation controls. Reuse
of replay-dialog art and controls, message lifetime/count, white text
substitution and project attack/farm wording are disclosed in the ledger.
These panels have not been calibrated against a matched reference screenshot.

This is the initial feedback slice of #58, not closure of its whole inventory.
Colour-blind variants are exposed for HUD player text via a URL, not a complete
sprite/world-health/minimap mode or options-screen setting. Wonder work remains
under #110 (the human has already replied “agreed, a then b”); objectives and
tech-tree surfaces remain #138, and options UI remains #141. Describe #110 as
an accepted decision awaiting implementation, rather than an unanswered blocker.

## Acceptance checklist

- [x] Fix teardown result semantics and add a no-command-on-rebuild regression.
- [x] Fix persistent notification stacking and verify visible background pixels.
- [x] Rerun imported/fallback feedback smoke and the full gate on the corrected
  tree; update #58 with those results.
