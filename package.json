{
  "name": "open-empires-lab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "tools:setup": "tools/bootstrap.sh",
    "import:militia": "tools/import_militia.sh",
    "test:import": ".tools/import-venv/bin/python -m unittest discover -s tools -p 'test_*.py' -v"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@types/three": "^0.180.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.7",
    "vitest": "^3.2.4"
  }
}
